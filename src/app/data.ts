export class Person {

  origine: string;
  number: number;
  time: number;

  // tslint:disable-next-line:variable-name
  constructor(origine: string, number: number, time: number) {
    this.origine = origine;
    this.number = number;
    this.time = time;
  }
}
